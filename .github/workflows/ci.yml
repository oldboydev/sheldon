name: Verify pull request

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    name: verify
    # The MVP and its process-isolation suite are Windows-first; running the
    # unified gate here keeps CI on the supported platform contract.
    runs-on: windows-2022
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 24.13.0
          cache: npm
      - run: npm ci
      - run: npm run verify
